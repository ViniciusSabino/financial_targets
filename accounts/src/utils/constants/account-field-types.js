const fieldTypes = {
    number: 'number',
    string: 'string',
    date: 'date',
    enum: 'enum',
    array: 'array',
    boolean: 'boolean',
    dateStart: 'dateStart',
    dateEnd: 'dateEnd',
};

export default fieldTypes;
