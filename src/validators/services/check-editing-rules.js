const checkEditingRules = async () => {
    // TODO:
    /*
     1 - Recupera a conta atual do state da request
     2 - Com base na conta atual e nos campos para editar, validar se a conta pode ser editada
     3 - Em caso de erros retornar os erros e a action (Baseado no valid-save-rules)
     4 - Em caso de sucesso dar o next
     5 - Atribuir no state como currentAccount
     */
};

export default checkEditingRules;
