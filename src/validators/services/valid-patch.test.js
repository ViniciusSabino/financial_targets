import validPatch from './valid-patch';

const ACTION = 'patch';

describe('Validators', () => {
    describe('Services', () => {
        describe('Valid Path', () => {
            it('should', () => {
                expect(1).toEqual(1);
            });
        });
    });
});
