const patch = async ({ id, body, currentAccount }) => {
    // TODO:
    /*
    1 - Com base no id, nos campos para editar e na conta atual montar um objeto para update de uma conta
    2 - Realizar as mudanças necessárias nos campos relacionados
    */
};

export default patch;
